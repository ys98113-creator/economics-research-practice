# Gold Layer — Analysis-Ready Outputs

Final, paper-ready data and results.

- `analysis/` — Jupyter notebooks, R scripts, Stata do-files
- `outputs/` — model results, regression tables, charts
- `tables/` — publication-ready tables (LaTeX, CSV)
