# Bronze Layer — Raw Data

Store all raw, unprocessed data here. Never modify files in this layer.

- `raw_data/` — manually downloaded CSVs, Excel files
- `api_pulls/` — data fetched from APIs (FRED, World Bank, etc.)
- `scraped/` — web-scraped data
